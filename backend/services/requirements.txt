# Core framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.0

# AWS
boto3==1.35.0
botocore==1.35.0

# Datadog observability
ddtrace==2.13.0
datadog==0.49.0

# HTTP client (for signal ingestion)
httpx==0.27.0

# Utilities
python-dotenv==1.0.1

# MiniMax (optional, only needs httpx above)
# No extra package needed — uses httpx directly
