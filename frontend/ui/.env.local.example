# Copy to .env.local and fill in. See comments for what each var does.
# Frontend env — same structure as backend for consistency.

# ── Backend API (required when using real backend) ───────────────────────────
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_USE_MOCK=false

# ── CopilotKit sidebar chat (optional) ─────────────────────────────────────────
ANTHROPIC_API_KEY=
